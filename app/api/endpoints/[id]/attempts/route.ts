import {db} from '@/lib/db';
import {ownEndpoint,apiError} from '@/lib/access';
export async function GET(_req:Request,{params}:{params:{id:string}}){try{const ep=await ownEndpoint(params.id);const since=new Date(Date.now()-86400000);const [attempts,total,success]=await Promise.all([db.deliveryAttempt.findMany({where:{endpointId:ep.id},orderBy:{createdAt:'desc'},take:100,include:{event:{select:{id:true,type:true,idempotencyKey:true}}}}),db.deliveryAttempt.count({where:{endpointId:ep.id,createdAt:{gte:since},status:{not:'SKIPPED_CIRCUIT_OPEN'}}}),db.deliveryAttempt.count({where:{endpointId:ep.id,createdAt:{gte:since},status:'SUCCESS'}})]);return Response.json({endpoint:ep,attempts,successRate:total?Math.round(success/total*100):null,total});}catch(e){return apiError(e);}}
